# 学生成绩管理与分析系统 —— 实验报告

## 一、项目概述

本项目实现了一个**学生成绩管理与分析系统**，支持学生成绩的增删改查、数据清洗、统计分析、可视化图表等功能。系统采用 **B/S 架构**，前端使用 **Bootstrap 5** 构建响应式界面，后端使用 **Flask** 框架处理 HTTP 路由，数据层基于 **Pandas + Matplotlib** 进行数据处理与可视化。

### 技术栈

| 层次 | 技术 | 说明 |
|------|------|------|
| 前端框架 | Bootstrap 5 + jQuery | 响应式 UI 组件库 |
| 可视化 | ECharts 5 + Matplotlib | 交互式图表 + 静态图表 |
| 模板引擎 | Jinja2 | Flask 内置模板引擎 |
| 后端框架 | Flask 3.1 | Python Web 微框架 |
| 数据处理 | Pandas 3.0 + NumPy | 数据清洗、统计分析 |
| 图表生成 | Matplotlib 3.10 | 柱状图、饼图 |

---

## 二、系统架构

```
┌─────────────────────────────────────────────────────────────┐
│                        浏览器端                              │
│  ┌──────────┬──────────┬──────────┬──────────┬──────────┐  │
│  │ 仪表盘   │ 成绩管理  │ 统计分析  │ 可视化   │ 数据清洗  │  │
│  └──────────┴──────────┴──────────┴──────────┴──────────┘  │
│                    Bootstrap 5 + ECharts                    │
├─────────────────────────────────────────────────────────────┤
│                     Flask 路由层 (app.py)                    │
│  GET / → dashboard  GET /grades → 成绩管理                  │
│  GET /analysis → 统计  GET /charts → 图表                   │
│  GET /clean → 清洗    POST /grades/add → 添加               │
│  POST /grades/modify  POST /grades/delete                   │
│  POST /grades/query   POST /clean/run                       │
├─────────────────────────────────────────────────────────────┤
│                  数据处理层 (DataProcesser.py)                │
│  StuData 类：CSV 读写、数据校验、成绩管理                    │
│  数据清洗、统计分析、Matplotlib 可视化                        │
├─────────────────────────────────────────────────────────────┤
│                    数据存储层                                 │
│  data/scores.csv (原始数据)                                  │
│  data/scores_clean.csv (清洗后数据)                          │
└─────────────────────────────────────────────────────────────┘
```

### 文件结构

```
StudentGradeManager/
├── app.py                          # Flask 主程序，路由 + 业务逻辑
├── main.py                         # 命令行入口（原有）
├── StudentData/
│   └── DataProcesser.py            # 数据处理核心类 StuData
├── templates/
│   ├── base.html                   # 基础模板（导航栏 + 页脚）
│   ├── dashboard.html              # 首页仪表盘
│   ├── grades.html                 # 成绩管理（CRUD）
│   ├── analysis.html               # 统计分析
│   ├── charts.html                 # 可视化图表
│   └── clean.html                  # 数据清洗
├── static/
│   ├── css/
│   │   └── appstyle.css            # 自定义样式（现代化 UI）
│   ├── js/
│   │   └── app.js                  # 全局交互脚本
│   └── images/
│       ├── average.png             # 各科平均分柱状图
│       └── pie.png                 # 成绩分布饼图
├── data/
│   ├── scores.csv                  # 原始成绩数据
│   ├── scores_clean.csv            # 清洗后数据
│   ├── average.png                 # Matplotlib 生成（备用）
│   └── pie.png                     # Matplotlib 生成（备用）
└── 实验报告.md                      # 本文件
```

---

## 三、功能模块详解

### 3.1 首页仪表盘 (`/`)

**功能：** 系统数据概览，展示核心指标。

- **统计卡片（6 个）：** 学生总数、总分合计、平均总分、最高总分、最低总分、全科及格率
- **各科平均分展示：** 语文、数学、英语分别显示平均分、及格率、优秀率
- **快速操作入口：** 一键跳转到成绩管理、统计分析、可视化图表、数据清洗

**技术实现：** 后端 `get_stats()` 函数从 `StuData` 的 DataFrame 中计算各项指标，返回字典给模板渲染。

### 3.2 成绩管理 (`/grades`)

**功能：** 学生成绩的完整 CRUD 操作。

| 操作 | 路由 | 请求方式 | 说明 |
|------|------|---------|------|
| 添加 | `/grades/add` | POST | 学号、姓名、科目、分数 |
| 查询 | `/grades/query` | POST | 按学号查询，模态框展示 |
| 修改 | `/grades/modify` | POST | 学号、科目、新分数 |
| 删除 | `/grades/delete` | POST | 按学号删除（单科/全部） |

- **数据校验：** 学号 8 位数字、分数 0-100、科目有效性
- **防重复：** 已有成绩不允许重复添加，提示使用修改功能
- **快速操作：** 表格中每行有查询和删除按钮
- **实时反馈：** AJAX 异步提交，页面自动刷新

### 3.3 统计分析 (`/analysis`)

**功能：** 多维度成绩分析。

- **全班统计：** 学生总数、总分数和、平均总分、最高/最低分
- **各科统计：** 平均分、最高分、最低分、及格率（含进度条）、优秀率
- **个人排名：** 按总分降序排列，前三名显示金银铜牌
- **筛选结果：**
  - 不及格学生：列出有科目不及格的学生及具体科目
  - 全优学生：三科均 ≥ 90 分的学生

### 3.4 可视化图表 (`/charts`)

**功能：** 成绩数据的可视化展示。

**Matplotlib 静态图表：**
- 各科平均分柱状图（带数值标签）
- 成绩等级分布饼图（不及格/及格/良好/优秀）

**ECharts 交互式图表：**
- 各科平均分对比柱状图（可 hover 查看详情）
- 成绩等级分布环形图（带百分比标签）
- 学生个人成绩对比分组柱状图（每名学生三门科目对比）

**性能优化：** 直接使用 DataFrame 计算数据生成图表，避免调用 `StuData` 的 `plt_average()` 和 `plt_pie()`（这两个方法内部会反复调用 `clean()` 导致性能问题）。

### 3.5 数据清洗 (`/clean`)

**功能：** 清洗原始数据中的脏数据。

**清洗步骤（复用 `StuData.clean()` 方法）：**

| 步骤 | 操作 | 说明 |
|------|------|------|
| 1 | 处理空值 | 删除包含 NaN 的行 |
| 2 | 去除异常 ID | 删除非 8 位数字的学号 |
| 3 | 删除重复学生 | 保留唯一学号记录 |
| 4 | 处理异常分数 | 超出 0-100 的分数修正为 0 |
| 5 | 格式化姓名 | 去除空格、转小写 |

清洗后数据保存至 `data/scores_clean.csv`，页面展示清洗结果表格。

---

## 四、数据处理核心 (DataProcesser.py)

`StuData` 类是系统的数据处理核心，提供以下功能：

### 数据管理

```python
class StuData:
    subjects = ["语文", "数学", "英语"]
    
    def __init__(self, path):       # 从 CSV 加载数据
    def get_df(self):               # 获取 DataFrame
    def save_df(self):              # 保存到 CSV
    def check(self, sid, sub, score, name):  # 数据格式校验
```

### 成绩操作

```python
    def add_score(self)             # 添加成绩
    def query(self, opt)            # 查询（单个/全部）
    def modify(self)                # 修改成绩
    def delete_all(self, sid)       # 删除全部成绩
    def delete_subject(self, sid, sub)  # 删除单科成绩
```

### 数据分析

```python
    def analyse_class(self)         # 全班统计分析
    def analyse_subject(self)       # 分科统计分析
    def analyse_student(self)       # 个人统计分析
    def filter(self)                # 筛选（不及格/高分学生）
```

### 数据清洗

```python
    def clean(self)                 # 完整数据清洗流程
```

### 可视化

```python
    def plt_average(self)           # 各科平均分柱状图
    def plt_pie(self)               # 成绩分布饼图
```

---

## 五、界面设计亮点

### 现代化 UI 设计

| 设计元素 | 实现方式 | 效果 |
|---------|---------|------|
| **渐变主题色** | `linear-gradient(135deg, #667eea, #764ba2)` | 紫蓝渐变主色调 |
| **渐变色统计卡片** | 6 种不同配色方案 | 快速区分不同类型的指标 |
| **毛玻璃效果** | `backdrop-filter: blur(10px)` | 半透明卡片背景 |
| **卡片悬停动画** | `transform: translateY(-4px)` | hover 上浮效果 |
| **按钮涟漪效果** | CSS `::after` 伪元素动画 | 点击水波纹反馈 |
| **入场动画** | `@keyframes fadeInUp / scaleIn` | 页面加载渐入效果 |
| **渐变表格头** | 表格头部渐变背景 | 突出表头层次 |
| **自定义滚动条** | `::-webkit-scrollbar` | 渐变紫色滚动条 |
| **CSS 变量体系** | `:root` 变量定义 | 统一管理主题色 |

### 响应式设计

- 使用 Bootstrap 5 网格系统（`col-xl-2 col-md-4 col-6`）
- 移动端自适应导航栏（`navbar-expand-lg`）
- 小屏幕下统计卡片自适应布局
- 图表容器响应式（`window.resize` 事件）

---

## 六、关键技术问题与解决方案

### 1. Matplotlib 中文显示

**问题：** Matplotlib 默认字体不支持中文，图表标题和标签显示为方块。

**解决：** 设置 `plt.rcParams['font.sans-serif']` 为 SimHei、Microsoft YaHei 等中文字体。

### 2. plt.show() 阻塞 Flask

**问题：** `StuData.plt_average()` 和 `plt.pie()` 调用 `plt.show()` 会阻塞 Flask 服务器。

**解决：** 设置 `matplotlib.use('Agg')` 使用非交互式后端，并在图表生成后调用 `plt.close()` 释放内存。

### 3. 图表页面性能问题

**问题：** 原代码调用 `StuData` 的图表方法，每次都会执行两次 `clean()`（重读 CSV + 写入），导致页面加载缓慢。

**解决：** 直接在 Flask 路由中用 DataFrame 数据生成 Matplotlib 图表，避免重复 I/O。

### 4. 图片 404 问题

**问题：** 图表保存到 `data/` 目录，但 Flask 只提供 `static/` 目录的静态文件服务。

**解决：** 将图表保存到 `static/images/`，使用 `url_for('static', filename='images/...')` 正确引用。

### 5. 空文件初始化

**问题：** `StuData.__init__()` 读取 CSV 时要求至少一行数据，空文件会报错。

**解决：** 在 `get_stu_data()` 中创建占位行，并在 `df_to_list()` 中过滤掉占位行。

---

## 七、运行说明

### 环境要求

- Python 3.8+
- Flask 3.x
- Pandas 3.x
- Matplotlib 3.x

### 启动方式

```bash
# 安装依赖
pip install flask pandas matplotlib

# 启动应用
python app.py

# 访问地址
http://127.0.0.1:5000
```

### 键盘快捷键

| 快捷键 | 功能 |
|--------|------|
| `Alt+1` | 跳转到仪表盘 |
| `Alt+2` | 跳转到成绩管理 |
| `Alt+3` | 跳转到统计分析 |
| `Alt+4` | 跳转到可视化图表 |
| `Alt+5` | 跳转到数据清洗 |

---

## 八、总结

本项目实现了一个功能完整的学生成绩管理与分析系统，涵盖了从数据录入、清洗、存储到分析、可视化的全流程。系统后端复用已有的 `StuData` 数据处理类，前端使用 Bootstrap 5 构建了 5 个核心页面，提供了良好的用户体验。

### 已实现功能

- [x] 学生成绩的增删改查（CRUD）
- [x] 数据格式校验与防重复
- [x] 多维度统计分析（全班/各科/个人）
- [x] 数据可视化（Matplotlib 静态图 + ECharts 交互式图表）
- [x] 数据清洗（空值处理、异常值修正、去重、格式化）
- [x] 响应式界面（适配桌面/平板/手机）
- [x] 现代化 UI 设计（渐变、动画、毛玻璃效果）
- [x] 键盘快捷键导航
- [x] 表单输入自动格式化与校验

### 待改进方向

- 用户认证与权限管理
- 数据导出（Excel/PDF）
- 批量导入学生数据
- 更多可视化图表类型（雷达图、折线图等）
- 数据库存储替代 CSV 文件